<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:sys="clr-namespace:System;assembly=netstandard">

    <!-- ================================ -->
    <!-- Premium Color Palette            -->
    <!-- ================================ -->
    
    <!-- Background Gradient Colors -->
    <Color x:Key="BackgroundDark">#0F0F23</Color>
    <Color x:Key="BackgroundMid">#1A1A2E</Color>
    <Color x:Key="BackgroundLight">#16213E</Color>
    
    <!-- Accent Gradient Colors -->
    <Color x:Key="AccentPrimary">#7C3AED</Color>
    <Color x:Key="AccentSecondary">#A855F7</Color>
    <Color x:Key="AccentTertiary">#C084FC</Color>
    
    <!-- Cyan/Teal Accent -->
    <Color x:Key="CyanPrimary">#06B6D4</Color>
    <Color x:Key="CyanSecondary">#22D3EE</Color>
    
    <!-- Success/Warning/Danger -->
    <Color x:Key="SuccessColor">#10B981</Color>
    <Color x:Key="WarningColor">#F59E0B</Color>
    <Color x:Key="DangerColor">#EF4444</Color>
    
    <!-- Surface Colors -->
    <Color x:Key="SurfaceColor">#1E1E3F</Color>
    <Color x:Key="CardColor">#252547</Color>
    <Color x:Key="CardHoverColor">#2D2D5A</Color>
    
    <!-- Text Colors -->
    <Color x:Key="TextPrimary">#FFFFFF</Color>
    <Color x:Key="TextSecondary">#A0A0C0</Color>
    <Color x:Key="TextMuted">#6B6B8D</Color>
    
    <!-- ================================ -->
    <!-- Gradient Brushes                 -->
    <!-- ================================ -->
    
    <LinearGradientBrush x:Key="BackgroundGradient" StartPoint="0,0" EndPoint="1,1">
        <GradientStop Color="{StaticResource BackgroundDark}" Offset="0"/>
        <GradientStop Color="{StaticResource BackgroundMid}" Offset="0.5"/>
        <GradientStop Color="{StaticResource BackgroundLight}" Offset="1"/>
    </LinearGradientBrush>
    
    <LinearGradientBrush x:Key="PrimaryGradient" StartPoint="0,0" EndPoint="1,1">
        <GradientStop Color="{StaticResource AccentPrimary}" Offset="0"/>
        <GradientStop Color="{StaticResource AccentSecondary}" Offset="1"/>
    </LinearGradientBrush>
    
    <LinearGradientBrush x:Key="CyanGradient" StartPoint="0,0" EndPoint="1,1">
        <GradientStop Color="{StaticResource CyanPrimary}" Offset="0"/>
        <GradientStop Color="{StaticResource CyanSecondary}" Offset="1"/>
    </LinearGradientBrush>
    
    <LinearGradientBrush x:Key="SuccessGradient" StartPoint="0,0" EndPoint="1,1">
        <GradientStop Color="#059669" Offset="0"/>
        <GradientStop Color="#10B981" Offset="1"/>
    </LinearGradientBrush>
    
    <LinearGradientBrush x:Key="WarningGradient" StartPoint="0,0" EndPoint="1,1">
        <GradientStop Color="#D97706" Offset="0"/>
        <GradientStop Color="#F59E0B" Offset="1"/>
    </LinearGradientBrush>
    
    <LinearGradientBrush x:Key="SidebarGradient" StartPoint="0,0" EndPoint="0,1">
        <GradientStop Color="#1A1A2E" Offset="0"/>
        <GradientStop Color="#0F0F23" Offset="1"/>
    </LinearGradientBrush>
    
    <!-- Solid Brushes -->
    <SolidColorBrush x:Key="WindowBackgroundBrush" Color="{StaticResource BackgroundDark}"/>
    <SolidColorBrush x:Key="CardBackgroundBrush" Color="{StaticResource CardColor}"/>
    <SolidColorBrush x:Key="CardHoverBrush" Color="{StaticResource CardHoverColor}"/>
    <SolidColorBrush x:Key="SurfaceBrush" Color="{StaticResource SurfaceColor}"/>
    <SolidColorBrush x:Key="TextPrimaryBrush" Color="{StaticResource TextPrimary}"/>
    <SolidColorBrush x:Key="TextSecondaryBrush" Color="{StaticResource TextSecondary}"/>
    <SolidColorBrush x:Key="TextMutedBrush" Color="{StaticResource TextMuted}"/>
    <SolidColorBrush x:Key="AccentBrush" Color="{StaticResource AccentPrimary}"/>
    <SolidColorBrush x:Key="SuccessBrush" Color="{StaticResource SuccessColor}"/>
    <SolidColorBrush x:Key="WarningBrush" Color="{StaticResource WarningColor}"/>
    <SolidColorBrush x:Key="DangerBrush" Color="{StaticResource DangerColor}"/>
    
    <!-- Glass Effect Brush -->
    <SolidColorBrush x:Key="GlassBrush" Color="#1AFFFFFF"/>
    <SolidColorBrush x:Key="GlassBorderBrush" Color="#3AFFFFFF"/>
    <SolidColorBrush x:Key="BackgroundBrush" Color="{StaticResource BackgroundDark}"/>

    <!-- ================================ -->
    <!-- Styling Values                   -->
    <!-- ================================ -->
    
    <CornerRadius x:Key="CardCornerRadius">16</CornerRadius>
    <CornerRadius x:Key="SmallCornerRadius">8</CornerRadius>
    <CornerRadius x:Key="ButtonCornerRadius">12</CornerRadius>
    <Thickness x:Key="CardPadding">20</Thickness>
    <Thickness x:Key="CardMargin">10</Thickness>
    <Thickness x:Key="ButtonPadding">20,12</Thickness>

</ResourceDictionary>
