<Window x:Class="ApliqxPos.Views.UpdateDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        Title="تحديث متاح" Height="420" Width="520"
        WindowStartupLocation="CenterOwner"
        Background="{DynamicResource BackgroundBrush}"
        Foreground="{DynamicResource TextBrush}"
        ResizeMode="NoResize"
        FlowDirection="RightToLeft">

    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,16">
            <materialDesign:PackIcon Kind="Update" Width="32" Height="32"
                                     Foreground="{DynamicResource AccentBrush}"
                                     VerticalAlignment="Center" Margin="0,0,12,0"/>
            <StackPanel>
                <TextBlock Text="🎉 يوجد تحديث جديد!" FontSize="18" FontWeight="Bold"/>
                <TextBlock Text="{Binding CurrentVersion}" Foreground="{DynamicResource TextMutedBrush}" FontSize="12"/>
            </StackPanel>
        </StackPanel>

        <!-- New Version Badge -->
        <Border Grid.Row="1" Background="{DynamicResource AccentBrush}" CornerRadius="8"
                Padding="12,8" Margin="0,0,0,16" HorizontalAlignment="Left">
            <StackPanel Orientation="Horizontal">
                <TextBlock Text="الإصدار الجديد: " Foreground="White" FontWeight="Bold"/>
                <TextBlock Text="{Binding NewVersion}" Foreground="White" FontWeight="Bold"/>
            </StackPanel>
        </Border>

        <!-- Release Notes -->
        <Border Grid.Row="2" Background="{DynamicResource SurfaceBrush}" CornerRadius="8" Padding="12" Margin="0,0,0,16">
            <ScrollViewer VerticalScrollBarVisibility="Auto">
                <TextBlock Text="{Binding ReleaseNotes}"
                           FontSize="13" TextWrapping="Wrap"
                           Foreground="{DynamicResource TextMutedBrush}"/>
            </ScrollViewer>
        </Border>

        <!-- Progress Bar -->
        <StackPanel Grid.Row="3" Margin="0,0,0,16"
                    Visibility="{Binding IsDownloading, Converter={StaticResource BoolToVisibilityConverter}}">
            <TextBlock Text="{Binding StatusMessage}" FontSize="13" Margin="0,0,0,8"
                       Foreground="{DynamicResource AccentBrush}"/>
            <ProgressBar Value="{Binding DownloadProgress}" Maximum="100" Height="8"
                         Style="{StaticResource MaterialDesignLinearProgressBar}"/>
        </StackPanel>

        <!-- Buttons -->
        <Grid Grid.Row="4">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <TextBlock Grid.Column="0" Text="{Binding StatusMessage}" VerticalAlignment="Center"
                       Foreground="{DynamicResource TextMutedBrush}" FontSize="12"
                       Visibility="{Binding IsDownloading, Converter={StaticResource InverseBoolToVisibilityConverter}}"/>

            <Button Grid.Column="1"
                    Content="لاحقاً" Click="LaterButton_Click"
                    Style="{StaticResource MaterialDesignOutlinedButton}"
                    IsEnabled="{Binding IsDownloading, Converter={StaticResource InverseBoolConverter}}"
                    Margin="0,0,12,0"/>

            <Button Grid.Column="2"
                    Content="تحديث الآن 🚀"
                    Command="{Binding DownloadAndInstallCommand}"
                    Style="{StaticResource GradientButton}"
                    IsEnabled="{Binding IsDownloading, Converter={StaticResource InverseBoolConverter}}"/>
        </Grid>
    </Grid>
</Window>

